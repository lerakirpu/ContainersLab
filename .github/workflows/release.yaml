name: 'Containers Lab'

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

env:
  VERSION: ${{ github.ref_name || '0.0.1' }}

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DVERSION=${{ env.VERSION }}

      - name: Build
        run: cmake --build build --config Release -j4

      - name: Run tests
        run: ./build/main 
        
      - name: Create DEB package
        run: |
          cmake --build build --target package

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-package
          path: build/*.deb

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DVERSION=${{ env.VERSION }}

      - name: Build
        run: cmake --build build --config Release

      - name: Run tests
        run: .\build\Release\main.exe

      - name: Create ZIP package
        run: |
          Compress-Archive -Path ./build/Release/*.exe -DestinationPath containers-${{ env.VERSION }}-windows.zip

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-package
          path: ./containers-${{ env.VERSION }}-windows.zip

  create-release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/linux-package/*
            artifacts/windows-package/*